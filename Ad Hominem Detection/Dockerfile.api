FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements-api.txt ./
RUN pip install --no-cache-dir -r requirements-api.txt

COPY api_service.py ./

EXPOSE 9000

CMD ["uvicorn", "api_service:app", "--host", "0.0.0.0", "--port", "9000"]
